<%-include ('../../partials/header.ejs') %> 
<body>
    <%-include ('../../partials/homenavbar.ejs') %>

    <div class="container">
        <hr>
        <form action="" method="post">

                <% titles.forEach(title => { %>

                    <div class="card mt-5">
                        <div class="card-header">
                            <h5><%= title.bodyTitle %></h5>
                        </div>
        
                        <div class="card-body">
                            <% options.forEach(option => { %>
                                <% if(option.TitlesQuestionIdTitle == title.idTitle) { %>

                                    <div class="mb-3">
                                        <div class="input-group mb-3">
                                            <div class="input-group">
                                                <div class="input-group-text">
                                                  <input class="form-check-input mt-0" name="<%= option.TitlesQuestionIdTitle %>" type="radio" value="0" aria-label="Radio button for following text input" required>
                                                </div>
                                                <input type="text" name="" class="form-control" value="<%= option.bodyOption %>">
                                            </div>
                                      </div>
                                    </div>

                                <% } %>
                            <% }) %>
                        </div>
                    </div>
                <% }) %>

                <div class="d-grid gap-2 mb-5 mt-5">
                    <button class="btn btn-success" type="button">Enviar avaliação!</button>
                </div>

        </form>

    </div>
</body>
</html>